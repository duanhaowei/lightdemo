<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
     http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

	<!-- 工作的bean -->
	<!-- job的配置开始 -->
	<!-- 
	方式一-->
	<bean id="exampleBusinessObject" class="com.lightdemo.rss.service.DeptAdminJob"></bean>
	<bean id="jobDetail"
		class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<property name="targetObject" ref="exampleBusinessObject"/>
		<property name="targetMethod" value="doIt" />
		<property name="concurrent" value="false" />
	</bean> 
	
	<bean id="simpleTrigger" class="org.springframework.scheduling.quartz.SimpleTriggerFactoryBean">
	    	 <!-- see the example of method invoking job above  -->
	    <property name="jobDetail" ref="jobDetail"/>
	    	<!-- 10 seconds  -->
	    <property name="startDelay" value="10000"/>
	    	<!-- repeat every 50 seconds  -->
	    <property name="repeatInterval" value="50000"/>
	</bean>
	
	
	<!-- 方式二 -->
	<!--  
	<bean name="orgAdminJob" class="org.springframework.scheduling.quartz.JobDetailFactoryBean">
	    <property name="jobClass" value="com.lightdemo.rss.service.CommonJob" />
	  	<property name="jobDataAsMap">
	        <map>
	            <entry key="timeout" value="5"/>
	            <entry key="newsService" value-ref="newsService"/>
	        </map>
	    </property>
	</bean>

	<bean id="cronTrigger"
		class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail" ref="orgAdminJob" />
		 run every morning at 6 AM 
		<property name="cronExpression" value="0 0 0 * * ?"/>
		<property name="cronExpression" value="0/5 * * * * ?"></property>  
	</bean>
	-->
	<!-- 启动触发器的配置开始 -->
	<bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="triggers">
			<list>
				<!-- <ref bean="cronTrigger" /> -->
				<ref bean="simpleTrigger" />
			</list>
		</property>
	</bean>
	<!-- 启动触发器的配置结束 -->
</beans>